Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 1].[CA_Database].dbo.member M inner join [Server 1].[ CA_Database].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union 

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 3].[FL_Database].dbo.member M inner join [Server 3].[FL_Server].dbo.Enrollkeys EK on M.memid=EK.memid


Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union 

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 1].[UNV_Database].dbo.member M inner join [Server 1].[ UNV_Database].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server WV01].[IL_Database].dbo.member M inner join [Server WV01].[ IL_Database].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 2].[MAPD_Database].dbo.member M inner join [Server 2].[ MAPD_Database].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 4].[ MI_Database].dbo.member M inner join [Server 4].[ MI_Database].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 2].[Database_NM].dbo.member M inner join [Server 2].[ Database_NM].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 2].[Database_OH].dbo.member M inner join [Server 2].[ Database_OH].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 1].[Database_PR].dbo.member M inner join [Server 1].[Database_PR].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union
Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 3].[Database_SC].dbo.member M inner join [Server_3].[Database_SC].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union
Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 4].[Database_TX].dbo.member M inner join [Server 4].[ Database_TX].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 2].[Database_UT].dbo.member M inner join [Server 2].[ Database_UT].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server 3].[Database_WA].dbo.member M inner join [Server 3].[Database_WA].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

Union

Select M.MemID,M.DOB,Ek.enrollid, Ek.carriermemid, EK.effdate, EK.segtype

From[Server W].[Database_WI].dbo.member M inner join[Server W].[ Database_WI].dbo.Enrollkeys EK on M.memid=EK.memid

Where effdate<= GetDate()
And termdate>= Getdate()
And segtype='INT'
AND effdate<DOB

